11.2-3
由于需要对链表排序
所以,          不排序     排序
    insert      O(1)     O(1+a)
    delete      O(1+a)   O(1+a)
   search失败   O(1+a)   O((1+a)/2)
   search成功   O(1+a)   O(1+a)

11.2-4
自由链表使用双向链表

11.2-6
注意这里的是设计题,设计一个程序查询时间期望是O(L(1+1/a));
而不是说,散列表已经在给出了,如果是这样的话,只需要O(L)的时间就能查到.